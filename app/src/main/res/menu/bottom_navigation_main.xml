<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    >

<item
    android:id="@+id/action_now"
    android:enabled="true"
    android:icon="@drawable/ic_sleep_now"
    android:title="Now"
    app:showAsAction="ifRoom"
    />

    <item
        android:id="@+id/action_schedule"
        android:enabled="true"
        android:icon="@drawable/ic_sleep_schedule"
        android:title="Schedule"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/action_songs"
        android:enabled="true"
        android:icon="@drawable/ic_sleep_songs"
        android:title="Music"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/action_tracking"
        android:enabled="true"
        android:icon="@android:drawable/ic_menu_my_calendar"
        android:title="Sleep-tracking"
        app:showAsAction="ifRoom"
        />


</menu>